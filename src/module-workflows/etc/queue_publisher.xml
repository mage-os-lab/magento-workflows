<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework-message-queue:etc/publisher.xsd">
    <!-- Default connection amqp; deployments without RabbitMQ override to db via env.php queue config -->
    <publisher topic="mageos.workflow.execute">
        <connection name="amqp" exchange="magento"/>
    </publisher>
    <publisher topic="mageos.workflow.resume">
        <connection name="amqp" exchange="magento"/>
    </publisher>
</config>
