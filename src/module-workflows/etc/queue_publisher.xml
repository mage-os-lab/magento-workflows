<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework-message-queue:etc/publisher.xsd">
    <!-- Default connection db (no-broker installs work out of the box; the first dispatch on a
         RabbitMQ-less install previously threw "Unknown connection name amqp"); deployments
         with RabbitMQ remap via env.php queue config. -->
    <publisher topic="mageos.workflow.execute">
        <connection name="db" exchange="magento"/>
    </publisher>
    <publisher topic="mageos.workflow.resume">
        <connection name="db" exchange="magento"/>
    </publisher>
</config>
