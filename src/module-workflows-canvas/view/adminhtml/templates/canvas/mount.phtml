<?php
/**
 * Copyright (c) Mage-OS. Licensed under OSL-3.0.
 * See LICENSE.txt for license details.
 *
 * @var \MageOS\WorkflowsCanvas\Block\Adminhtml\Canvas\Mount $block
 * @var \Magento\Framework\Escaper $escaper
 *
 * CSP-safe mount point. Config reaches the React bundle ONLY through the
 * data-config attribute below (escaped as an HTML attribute) — there is no
 * inline <script>, no server-generated JS. The bundle is a self-hosted IIFE
 * loaded from pub/static (same-origin, allowed by script-src 'self').
 */
$escaper = $escaper ?? $block->getEscaper();
?>
<link rel="stylesheet" type="text/css"
      href="<?= $escaper->escapeUrl($block->getViewFileUrl('MageOS_WorkflowsCanvas::js/dist/canvas.css')) ?>"/>
<div class="mageos-workflows-canvas-page">
    <div id="mageos-workflows-canvas-root"
         class="mageos-workflows-canvas-root"
         data-role="mageos-workflows-canvas"
         data-config="<?= $escaper->escapeHtmlAttr($block->getConfigJson()) ?>">
        <noscript>
            <div class="message message-notice">
                <?= $escaper->escapeHtml(__('The visual canvas requires JavaScript. The classic form and JSON editor remain fully supported without it.')) ?>
            </div>
        </noscript>
    </div>
</div>
<script type="text/javascript"
        src="<?= $escaper->escapeUrl($block->getViewFileUrl('MageOS_WorkflowsCanvas::js/dist/canvas.js')) ?>"></script>
