<?php
declare(strict_types=1);

namespace MageOS\WorkflowsCanvas\Model\I18n;

/**
 * The canvas UI phrase catalog: every English phrase the React app passes to
 * its t() helper, mapped to its translation through Magento's own `__()` — one
 * call per phrase, so the standard translation pipeline (theme/module i18n
 * CSVs, database translations, locale fallback) stays fully authoritative and
 * the repo's phrase collector (dev/tools/i18n-collect-phrases.php) sees every
 * row as a normal PHP call site.
 *
 * Mount.php injects all() into the bootstrap config as `i18n`; the app installs
 * the map once at mount (app/src/i18n.ts) and renders nothing but lookups. In
 * en_US every value equals its key, and the client falls back to the English
 * key for any phrase missing here — so an out-of-date catalog degrades to
 * untranslated text, never to broken UI.
 *
 * KEEP IN SYNC with the t() literals under app/src — the collector's --check
 * mode regenerates i18n/en_US.csv from BOTH sides of this seam.
 */
class PhraseCatalog
{
    /**
     * @return array<string, string> English phrase => translated phrase
     */
    public function all(): array
    {
        return [
            '(not offered)' => (string) __('(not offered)'),
            '(not set)' => (string) __('(not set)'),
            '(secret)' => (string) __('(secret)'),
            '(unreadable condition tree)' => (string) __('(unreadable condition tree)'),
            'A case key is required.' => (string) __('A case key is required.'),
            'A condition tree must be a JSON object.' => (string) __('A condition tree must be a JSON object.'),
            'A cron expression in the store\'s timezone, e.g. 0 3 * * * for daily at 03:00.' => (string) __('A cron expression in the store\'s timezone, e.g. 0 3 * * * for daily at 03:00.'),
            'A date (YYYY-MM-DD) or a relative expression like "-30 days" / "+2 hours", resolved at evaluation time.' => (string) __('A date (YYYY-MM-DD) or a relative expression like "-30 days" / "+2 hours", resolved at evaluation time.'),
            'A duration is required.' => (string) __('A duration is required.'),
            'A key is required.' => (string) __('A key is required.'),
            'A label is required.' => (string) __('A label is required.'),
            'a moment' => (string) __('a moment'),
            'A name is required before saving.' => (string) __('A name is required before saving.'),
            'A switch needs at least one case.' => (string) __('A switch needs at least one case.'),
            'a while' => (string) __('a while'),
            'Action' => (string) __('Action'),
            'Action code not registered — deletable, not configurable.' => (string) __('Action code not registered — deletable, not configurable.'),
            'Add' => (string) __('Add'),
            'Add a condition to' => (string) __('Add a condition to'),
            'Add case' => (string) __('Add case'),
            'Add conditions' => (string) __('Add conditions'),
            'Add payload field' => (string) __('Add payload field'),
            'Add recipient' => (string) __('Add recipient'),
            'against' => (string) __('against'),
            'Aggregator' => (string) __('Aggregator'),
            'ALL' => (string) __('ALL'),
            'ALL of them match' => (string) __('ALL of them match'),
            'Allow bulk decisions' => (string) __('Allow bulk decisions'),
            'always' => (string) __('always'),
            'Always matches' => (string) __('Always matches'),
            'amount' => (string) __('amount'),
            'An email address is required.' => (string) __('An email address is required.'),
            'ANY' => (string) __('ANY'),
            'ANY of them matches' => (string) __('ANY of them matches'),
            'Apply' => (string) __('Apply'),
            'Approval' => (string) __('Approval'),
            'Approval gate' => (string) __('Approval gate'),
            'approved' => (string) __('approved'),
            'Assignee role' => (string) __('Assignee role'),
            'Attribute' => (string) __('Attribute'),
            'auto' => (string) __('auto'),
            'Available variables' => (string) __('Available variables'),
            'Branch' => (string) __('Branch'),
            'Branch (if/else)' => (string) __('Branch (if/else)'),
            'Cancel' => (string) __('Cancel'),
            'case' => (string) __('case'),
            'Case key' => (string) __('Case key'),
            'Cases' => (string) __('Cases'),
            'cases' => (string) __('cases'),
            'Cases (first match wins)' => (string) __('Cases (first match wins)'),
            'Choose a registered event instead' => (string) __('Choose a registered event instead'),
            'Choose an entity type in Workflow settings first — the available condition attributes depend on it.' => (string) __('Choose an entity type in Workflow settings first — the available condition attributes depend on it.'),
            'Clear overlay' => (string) __('Clear overlay'),
            'Click to edit the workflow conditions' => (string) __('Click to edit the workflow conditions'),
            'Close' => (string) __('Close'),
            'Condition' => (string) __('Condition'),
            'condition' => (string) __('condition'),
            'Condition metadata could not be loaded.' => (string) __('Condition metadata could not be loaded.'),
            'Condition metadata is unavailable.' => (string) __('Condition metadata is unavailable.'),
            'Condition tree (JSON)' => (string) __('Condition tree (JSON)'),
            'Conditions' => (string) __('Conditions'),
            'Conditions must be valid JSON (a serialized condition tree).' => (string) __('Conditions must be valid JSON (a serialized condition tree).'),
            'Conditions set' => (string) __('Conditions set'),
            'contains' => (string) __('contains'),
            'Count business days only (Mon–Fri, store timezone)' => (string) __('Count business days only (Mon–Fri, store timezone)'),
            'day' => (string) __('day'),
            'days' => (string) __('days'),
            'default' => (string) __('default'),
            'Delay' => (string) __('Delay'),
            'Delete step' => (string) __('Delete step'),
            'does not contain' => (string) __('does not contain'),
            'Dry-run failed.' => (string) __('Dry-run failed.'),
            'Dry-run preview' => (string) __('Dry-run preview'),
            'Duration' => (string) __('Duration'),
            'e.g. to_status or items.0.sku' => (string) __('e.g. to_status or items.0.sku'),
            'Edit as JSON' => (string) __('Edit as JSON'),
            'Edit conditions…' => (string) __('Edit conditions…'),
            'Editor actions' => (string) __('Editor actions'),
            'Enter an event name instead' => (string) __('Enter an event name instead'),
            'Enter an ISO-8601 duration instead' => (string) __('Enter an ISO-8601 duration instead'),
            'Entity ID' => (string) __('Entity ID'),
            'Entity type' => (string) __('Entity type'),
            'entry' => (string) __('entry'),
            'Entry step' => (string) __('Entry step'),
            'equals or greater than' => (string) __('equals or greater than'),
            'equals or less than' => (string) __('equals or less than'),
            'Event name' => (string) __('Event name'),
            'Execution' => (string) __('Execution'),
            'Execution steps request failed.' => (string) __('Execution steps request failed.'),
            'Existence' => (string) __('Existence'),
            'EXISTS' => (string) __('EXISTS'),
            'Expected result' => (string) __('Expected result'),
            'Failed to load execution.' => (string) __('Failed to load execution.'),
            'FALSE' => (string) __('FALSE'),
            'Flow' => (string) __('Flow'),
            'greater than' => (string) __('greater than'),
            'Hold Ctrl (⌘ on macOS) to select more than one.' => (string) __('Hold Ctrl (⌘ on macOS) to select more than one.'),
            'hour' => (string) __('hour'),
            'hours' => (string) __('hours'),
            'If' => (string) __('If'),
            'Instructions' => (string) __('Instructions'),
            'is' => (string) __('is'),
            'is not' => (string) __('is not'),
            'is not one of' => (string) __('is not one of'),
            'is one of' => (string) __('is one of'),
            'ISO-8601, e.g. PT1H' => (string) __('ISO-8601, e.g. PT1H'),
            'key' => (string) __('key'),
            'Label' => (string) __('Label'),
            'less than' => (string) __('less than'),
            'Loading condition metadata…' => (string) __('Loading condition metadata…'),
            'Manual run' => (string) __('Manual run'),
            'Match mode' => (string) __('Match mode'),
            'minute' => (string) __('minute'),
            'minutes' => (string) __('minutes'),
            'more' => (string) __('more'),
            'Move case earlier:' => (string) __('Move case earlier:'),
            'Move case later:' => (string) __('Move case later:'),
            'Move payload field down:' => (string) __('Move payload field down:'),
            'Move payload field up:' => (string) __('Move payload field up:'),
            'Name' => (string) __('Name'),
            'next' => (string) __('next'),
            'No' => (string) __('No'),
            'no' => (string) __('no'),
            'No conditions — this always runs.' => (string) __('No conditions — this always runs.'),
            'No workflow definition to display. The classic form and JSON editor remain the primary authoring surface.' => (string) __('No workflow definition to display. The classic form and JSON editor remain the primary authoring surface.'),
            'NONE of them matches' => (string) __('NONE of them matches'),
            'Not a switch step.' => (string) __('Not a switch step.'),
            'Not an ISO-8601 duration (e.g. PT30M, PT2H, P3D).' => (string) __('Not an ISO-8601 duration (e.g. PT30M, PT2H, P3D).'),
            'NOT EXISTS' => (string) __('NOT EXISTS'),
            'NOT EXISTS is a bare existence check — child conditions are not allowed.' => (string) __('NOT EXISTS is a bare existence check — child conditions are not allowed.'),
            'Not set' => (string) __('Not set'),
            'Notify email' => (string) __('Notify email'),
            'Notify emails' => (string) __('Notify emails'),
            'Number' => (string) __('Number'),
            'of these conditions are' => (string) __('of these conditions are'),
            'of:' => (string) __('of:'),
            'On by default: the entity is re-hydrated fresh so a stale snapshot (after a delay, queue lag or redelivery) cannot decide the branch. Turn it off to evaluate the frozen trigger snapshot instead.' => (string) __('On by default: the entity is re-hydrated fresh so a stale snapshot (after a delay, queue lag or redelivery) cannot decide the branch. Turn it off to evaluate the frozen trigger snapshot instead.'),
            'on event' => (string) __('on event'),
            'on timeout' => (string) __('on timeout'),
            'Only if' => (string) __('Only if'),
            'Operator for' => (string) __('Operator for'),
            'Operator for the trigger payload path' => (string) __('Operator for the trigger payload path'),
            'Optional. After the duration, roll forward to the next occurrence of this time.' => (string) __('Optional. After the duration, roll forward to the next occurrence of this time.'),
            'Other' => (string) __('Other'),
            'Outline' => (string) __('Outline'),
            'Overlay:' => (string) __('Overlay:'),
            'Payload field key' => (string) __('Payload field key'),
            'Payload field label' => (string) __('Payload field label'),
            'Payload field type' => (string) __('Payload field type'),
            'Payload fields' => (string) __('Payload fields'),
            'Re-validate entity before evaluating' => (string) __('Re-validate entity before evaluating'),
            'Redo' => (string) __('Redo'),
            'rejected' => (string) __('rejected'),
            'Related' => (string) __('Related'),
            'Related child aggregator' => (string) __('Related child aggregator'),
            'Relation' => (string) __('Relation'),
            'Reload execution' => (string) __('Reload execution'),
            'Remove all conditions' => (string) __('Remove all conditions'),
            'Remove case:' => (string) __('Remove case:'),
            'Remove condition:' => (string) __('Remove condition:'),
            'Remove notify email:' => (string) __('Remove notify email:'),
            'Remove payload field:' => (string) __('Remove payload field:'),
            'Remove:' => (string) __('Remove:'),
            'Retry' => (string) __('Retry'),
            'Required' => (string) __('Required'),
            'Required. No indefinite parks — the on-timeout path fires when nobody decides.' => (string) __('Required. No indefinite parks — the on-timeout path fires when nobody decides.'),
            'Required. The on-timeout path fires when the event has not arrived by then.' => (string) __('Required. The on-timeout path fires when the event has not arrived by then.'),
            'Run at (store-local HH:MM)' => (string) __('Run at (store-local HH:MM)'),
            'Run dry-run overlay' => (string) __('Run dry-run overlay'),
            'Save' => (string) __('Save'),
            'Saving…' => (string) __('Saving…'),
            'Schedule' => (string) __('Schedule'),
            'Schedule (cron)' => (string) __('Schedule (cron)'),
            'Search' => (string) __('Search'),
            'Searching…' => (string) __('Searching…'),
            'Search…' => (string) __('Search…'),
            'second' => (string) __('second'),
            'seconds' => (string) __('seconds'),
            'Set' => (string) __('Set'),
            'shown read-only and preserved exactly as stored.' => (string) __('shown read-only and preserved exactly as stored.'),
            'Shown read-only — edit it in the JSON editor.' => (string) __('Shown read-only — edit it in the JSON editor.'),
            'start' => (string) __('start'),
            'Status' => (string) __('Status'),
            'Step configuration' => (string) __('Step configuration'),
            'Step palette' => (string) __('Step palette'),
            'Stop' => (string) __('Stop'),
            'Switch' => (string) __('Switch'),
            'Switch (multi-way)' => (string) __('Switch (multi-way)'),
            'Text' => (string) __('Text'),
            'That does not look like an email address.' => (string) __('That does not look like an email address.'),
            'the attribute' => (string) __('the attribute'),
            'The condition metadata for this workflow\'s entity type is unavailable, so the builder cannot start a tree. Use “Edit as JSON” below.' => (string) __('The condition metadata for this workflow\'s entity type is unavailable, so the builder cannot start a tree. Use “Edit as JSON” below.'),
            'The condition validator could not be reached, so the tree was applied unvalidated. It is re-validated when the workflow is saved.' => (string) __('The condition validator could not be reached, so the tree was applied unvalidated. It is re-validated when the workflow is saved.'),
            'The conditions could not be validated.' => (string) __('The conditions could not be validated.'),
            'The server rejected this condition tree.' => (string) __('The server rejected this condition tree.'),
            'This action code is not registered on this install. It can be deleted but not configured.' => (string) __('This action code is not registered on this install. It can be deleted but not configured.'),
            'this canvas understands schema' => (string) __('this canvas understands schema'),
            'This case key is already used by this switch:' => (string) __('This case key is already used by this switch:'),
            'This duration mixes units, so it is edited as ISO-8601.' => (string) __('This duration mixes units, so it is edited as ISO-8601.'),
            'This key is used more than once:' => (string) __('This key is used more than once:'),
            'This switch has no cases yet — it cannot be saved until it has one.' => (string) __('This switch has no cases yet — it cannot be saved until it has one.'),
            'This workflow declares schema' => (string) __('This workflow declares schema'),
            'timeout' => (string) __('timeout'),
            'Timeout' => (string) __('Timeout'),
            'Title' => (string) __('Title'),
            'Trigger' => (string) __('Trigger'),
            'Trigger data' => (string) __('Trigger data'),
            'Trigger event' => (string) __('Trigger event'),
            'Trigger payload path (dot notation)' => (string) __('Trigger payload path (dot notation)'),
            'Trigger type' => (string) __('Trigger type'),
            'TRUE' => (string) __('TRUE'),
            'Type at least' => (string) __('Type at least'),
            'Unavailable' => (string) __('Unavailable'),
            'unavailable action' => (string) __('unavailable action'),
            'Undo' => (string) __('Undo'),
            'unit' => (string) __('unit'),
            'Unknown step' => (string) __('Unknown step'),
            'Unrecognized condition' => (string) __('Unrecognized condition'),
            'up to' => (string) __('up to'),
            'Use 24-hour HH:MM, e.g. 09:30.' => (string) __('Use 24-hour HH:MM, e.g. 09:30.'),
            'Use letters, numbers or "_" (max 64 characters).' => (string) __('Use letters, numbers or "_" (max 64 characters).'),
            'Use letters, numbers, "_" or "-" (max 64 characters).' => (string) __('Use letters, numbers, "_" or "-" (max 64 characters).'),
            'Use lower-case letters, numbers, “.”, “_” or “-” (max 128 characters).' => (string) __('Use lower-case letters, numbers, “.”, “_” or “-” (max 128 characters).'),
            'Use the duration picker' => (string) __('Use the duration picker'),
            'Validating…' => (string) __('Validating…'),
            'Validation errors — see the badged steps.' => (string) __('Validation errors — see the badged steps.'),
            'Validation request failed.' => (string) __('Validation request failed.'),
            'Value for' => (string) __('Value for'),
            'Value for the trigger payload path' => (string) __('Value for the trigger payload path'),
            'Values the decider fills in. Each key is exposed to later steps.' => (string) __('Values the decider fills in. Each key is exposed to later steps.'),
            'Wait' => (string) __('Wait'),
            'Wait for' => (string) __('Wait for'),
            'Wait for event' => (string) __('Wait for event'),
            'Websites' => (string) __('Websites'),
            'When' => (string) __('When'),
            'where' => (string) __('where'),
            'Workflow' => (string) __('Workflow'),
            'Workflow conditions' => (string) __('Workflow conditions'),
            'Workflow outline' => (string) __('Workflow outline'),
            'Workflow settings' => (string) __('Workflow settings'),
            'Workflow visual editor' => (string) __('Workflow visual editor'),
            'Yes' => (string) __('Yes'),
            'yes' => (string) __('yes'),
            'Yes / No' => (string) __('Yes / No'),
            '— add condition —' => (string) __('— add condition —'),
            '— add —' => (string) __('— add —'),
            '— select attribute —' => (string) __('— select attribute —'),
            '— select relation —' => (string) __('— select relation —'),
            '— select —' => (string) __('— select —'),
        ];
    }
}
