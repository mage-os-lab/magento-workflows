<?xml version="1.0"?>
<!--
/**
 * Copyright (c) Mage-OS. Licensed under OSL-3.0.
 * See LICENSE.txt for license details.
 */
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">
    <!-- Grid data sources and the dry-run provider pool are declared in the module's
         global etc/di.xml, NOT here: area DI is layered onto global config with a
         shallow array_replace(), so an array argument declared in this scope replaces
         the global one wholesale instead of merging into it. See etc/di.xml. -->

    <!-- Mass actions operate on the full domain collection (peer: MageOS_Workflows).
         Scalar object arguments, safe to bind per-area. -->
    <type name="MageOS\WorkflowsAdminUi\Controller\Adminhtml\Workflow\MassEnable">
        <arguments>
            <argument name="collectionFactory" xsi:type="object">MageOS\Workflows\Model\ResourceModel\Workflow\CollectionFactory</argument>
        </arguments>
    </type>
    <type name="MageOS\WorkflowsAdminUi\Controller\Adminhtml\Workflow\MassDisable">
        <arguments>
            <argument name="collectionFactory" xsi:type="object">MageOS\Workflows\Model\ResourceModel\Workflow\CollectionFactory</argument>
        </arguments>
    </type>
</config>
