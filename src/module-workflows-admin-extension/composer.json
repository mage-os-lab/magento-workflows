{
    "name": "mage-os/workflows-admin-extension",
    "description": "Mage-OS Workflow Engine: surfaces workflows on the native admin grid pages (Orders, Customers, Products, …) — a compact 'N active for Orders · View · Create workflow' strip beside each grid, so the feature is discoverable where the entity lives",
    "type": "magento2-module",
    "license": "OSL-3.0",
    "require": {
        "php": "~8.1.0||~8.2.0||~8.3.0||~8.4.0",
        "magento/framework": ">=103.0.4",
        "mage-os/workflows": "*",
        "mage-os/workflows-admin-ui": "*"
    },
    "autoload": {
        "files": ["registration.php"],
        "psr-4": {
            "MageOS\\WorkflowsAdminExtension\\": ""
        }
    }
}
